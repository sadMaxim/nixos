# OpenCode Tooling Rules
- This is a public repository; be careful never to add secrets, credentials, tokens, or other sensitive values here.

## Flake Updates
- **Always ask me before running `nix flake update`** or updating any flake inputs. Get explicit confirmation before making changes that could download large updates.
